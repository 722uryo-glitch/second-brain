# Durable Jobs: 運用と検証

## 今回の変更

- Job取得をSQLiteの短い排他トランザクションで行い、同時に実行できるJobは1件とする。
- 起動時に有効な実行権を取り消さない。異常終了はlease期限後に回収、正常終了は子処理を待ってから復旧する。
- 取得のたびに新しいworker tokenを発行し、heartbeat・工程保存・最終保存で確認する。
- 計画、会話状態、記憶、個別検索、証拠評価、調査パック、下書き、監査、修正、最終回答を保存する。
- 完了した工程は再利用し、未完了工程から再実行する。工程入力が変わった場合は誤再利用をせず停止する。
- 工程出力とartifactと完了イベントは同じトランザクションで保存する。
- 会話保存・Agent Run作成の二重書き込みをJob単位で抑止する。
- 取消は即座にterminal状態とし、古いWorkerの書き込みを拒否する。検索の兄弟タスクもcancelしてawaitする。
- 時間、HTTP/LLM試行数、再試行数、最大出力トークン予約をJob全体で共有する。
- 監査不合格をsucceededにしない。予算切れでも途中成果は保存される。
- UIは依頼・現在工程・確認済み/不足事項・途中成果を表示し、再読込時に最近20件の完了/未完了Jobを復元する。

## 更新

起動用フォルダーでアプリを停止し、通常どおり `./update.ps1`、`./run.ps1` を実行する。
DB変更は追加テーブル・追加列のみ。既存の会話や記憶を削除しない。
旧バージョンで開始したJobには工程出力がないため、そのJobの最初の再開だけは従来工程を再実行する。
新旧アプリを同じDBで同時に動かさない。

## 予算の意味

既定は1 Jobにつき900秒（JOB_MAX_RUNTIME_SECONDS）、120試行、12再試行、最大出力予約40,000 tokens。
最初の実行からの期限を永続化するため、PC停止時間も900秒に含む。長時間のPC停止後はpartialとなり、UIから明示的に追加して再開する。
HTTP/モデルの各試行、モデル切替、失敗工程の再実行が共通予算を消費する。失敗したモデル呼び出しの出力予約も戻さず保守的に数える。
「予算を追加して続きから再開」は900秒（そのJobの設定時間）、120試行、12再試行、40,000 tokensを追加する。自動追加はしない。
これはリソース予算であり、請求額の上限保証ではない。入力・推論トークンとモデル別料金の計算は未実装。
通常会話、定期収集はこのJob予算とは別。クラウド側が取消後も処理を継続するかはproviderに依存する。

## 再開の境界

- 完了した検索クエリは再利用する。クエリ内のHTTP処理の途中で落ちた場合、その未完了クエリは再実行する。
- LLMの生成途中のトークン列からの再開は行わない。最後に保存した工程境界から再実行する。
- 外部APIの応答後・工程保存前の異常終了では、その未保存API呼び出しは再発行され得る（at-least-once）。外部課金のexactly-onceは保証しない。
- 再開は同じ依頼・保存済み資料での続行。監査まで終えた根拠不足のJobを再開しても新資料は増えないため、エラーなしの最終回答済みJobは再開させない。
- 保存した下書き・調査パックは未検証内容を含む。artifact閲覧は監査合格を意味しない。
- 将来工程契約を変更するときはcheckpointバージョンと移行方針を同時に変更する。

## 動作確認

自動テスト: `python -m unittest discover -s tests -p 'test_*.py' -v`
構文検証: `python -m compileall -q app`

テストには、子プロセスの強制終了後の復旧、実行権の競合、取消後の保存拒否、兄弟タスクの終了、共有予算、実Executiveの下書き再開、HTTP API、UI JavaScript構文を含む。
テストでは一時DBと偽のモデル/検索応答を用い、本番DBや課金APIを使わない。
CIはUbuntuとWindowsのPython 3.11で実行する。

手動確認:
1. 調査依頼を送信し、受付・工程・途中成果を確認する。
2. 下書き以降でアプリを正常停止し再起動する。保存済み工程が再利用されることを確認する。
3. 別の依頼を取消し、状態が完了に書き換わらないことを確認する。
4. 画面を再読込し、完了結果と途中成果が復元されることを確認する。

## 今後の別課題

Evidence Gapの専用データ不足/公開情報不足などの分類、SEO専用データ接続、実請求額によるルーティング、外部投稿やPC操作は今回の変更には含めない。
無料モデルや検索サービスの応答品質・実測速度は、設定済みサービスで別途評価する。
