# 自主的に情報を集め、知識を育てる

## 動作

アプリ起動中、10分ごとに探索課題を確認します。初期テーマは AI agents / knowledge management / Obsidian。画面で公開検索テーマを変更できます。
1日に最大12回、各回最大10分・40呼び出し・8再試行・24,000出力トークンの予約を与えます。失敗後の再開も12回に含みます。
自主探索のAIはローカルOllamaのみで、クラウドへのfallbackを禁止します。既存の通常チャットのクラウド設定は変更しません。
アプリ終了中は動作しません。通常の重い依頼が来たら、自主探索を中断して保存済み工程から後で再開します。

- 関心分野を検索し、結果を保存します。
- 取得済み本文を5,000文字ずつ読み、複数の主張・意見・仮説と原文の引用位置を残します。
- 古いノートも検索対象にして、意味のある関連をAIが検討します。両ノートの引用が一致する関連だけを「仮説」として保存します。
- 公開資料の読み取りから疑問を生成し、自動探索キューへ戻します。個人メモを含む関連付け結果からは検索語を生成しません。
- 再帰探索は2段階、未処理の疑問は100件、巡回からの解読待ちは200件を上限とします。
- 検索に成功した条件と固定の調査手順を保存し、画面から同じ条件で新たに調べ直せます。
- 基本テーマは7日後に再探索します。前回の検索結果のキャッシュを使わず、新しいJobを作ります。

## Obsidian

Vault内の `Second Brain/HOME.md` が入口です。

- `Knowledge/`：出所・読み込み範囲・複数の主張・関連ノートへのリンク。
- `Resurfaced.md`：新情報によって再浮上した過去のノート。
- `Questions.md`：自動で調べる疑問と現在の状態。
- `Workflows.md`：再利用できる検索条件と固定手順。
- `Inbox/`：自由に書いたMarkdownメモをローカルで取り込みます。

既存の明示的なメモ・目標・決定も照合候補になります。個人メモはprivateとして保持します。
生成済みノートを編集した場合、上書きせず私的な訂正ノートとして取り込みます。
AIが作った新しい版は `.pending-...md` に保存します。元資料の解釈を人の編集で黙って置き換えません。
通常のObsidian出力周期、または画面の「Obsidianへ反映」で更新できます。

## 失敗と限界

解読のJSONが壊れている場合や原文引用が一致しない場合、記事タイトルを支持証拠として登録する旧fallbackは使いません。
読み取りは途中状態に残り、30分以上の間隔を置いて最大3回まで再開します。完了済み区間は再読しません。
分類は原文中の主張を抽出するもので、真実の認定ではありません。旧方式で保存済みのClaimは自動で再検証していません。
読み込み範囲は「保存済み本文」です。取得先の制約、抽出失敗、最大文字数により、原資料全体が取得できたことを保証しません。
関連付けは仮説で、誤り得ます。ノート更新時には古い関連をstaleにし、現在の関連から外します。過去版をDBへ保存します。
ワークフローは成功した検索条件＋固定手順の再利用です。未知の手順を自動設計してコード化する機能ではありません。
関連候補検索は語句の一致と保存済みEmbeddingを使います。全ノートの高度なグラフ推論や、完全な多言語同義語統合は未実装です。

## 設定

`AUTONOMY_ENABLED=true`、`AUTONOMY_TOPICS=...`、`AUTONOMY_INTERVAL_SECONDS=600`、`AUTONOMY_DAILY_JOBS=12`。
画面から保存した有効/無効とテーマはDBへ永続化され、環境設定より優先します。
画面の一時停止は自主探索Jobだけを取り消します。既存のニュース定期収集設定、ユーザーのJobは変更しません。
新たな有料API・契約は不要です。ローカル実行の電力・計算資源と、既存ネットワーク接続は使います。

## 検証

`python -m unittest discover -s tests -p 'test_*.py' -v`

一時DB・一時Vault・偽のモデル/検索を使い、検索→解読→関連付け→追加検索、引用拒否、全文末尾の処理、途中再開、古いメモの再浮上、人の編集保護、競合・日次上限・通常依頼の優先、クラウド禁止を確認します。
実機のqwen3:8bでも架空の短い検証文の解読を確認しました。実際のニュース全体の分類精度を保証する評価ではありません。
